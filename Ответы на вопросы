1)Event Propagation это - Механизм, по которому события распространяються по DOM дереву.
есть 2 фазы 
	-Погружения 
		*window -> document -> body -> html -> div -> p
			если {capture:true} и происходит клик на button на котором навешан обработчик событии, то начнется фаза погружения до того момента пока обработчик не захватит.
			удобно например для проверки авторизации.
			предотвратить событие e.stopPropogation(), если условно пользователь ввел не правильный логин или пароль.
	-Всплытие
		*p -> div -> html -> body -> document -> window
			всплытие - когда есть модальное окно, при клике на фон, окно должна закрыться, но модальное окно закрывается, когда клик происходит на него.
			Для того, чтобы предотвратить такое поведение нужно указать e.stopPropogation(), чтобы событие дальше не всплывало на верх и не закрыло модальное окно. 
2)Promise - это объект , который предостовляет результат асинхронной операции.
Грубо говоря это обещание, что код выполниться и после выполнения можно обрабатывать данные 
основные состояния Promise 
	-ожидание(Pending)
	-успешно(Fulfilled)
	-отклонено(rejeckted)
Можно также обрабатывать асинхронный код с async await.

Event loop в javascript это механизм , который следит или выполняет код по очереди.
Event loop позволяет запускать асинхронный код позже, когда основной код уже выполнен.

Принцип работы event loop
	-call stack
		*Сюда попадают задачи 
		*Выполняются строго по очереди
	-Web APIs
	 Это то, то браузер делает асинхронно
		*fetch
		*setTimeout
		*обработчики событий
		*DOM операции 
Когда таймер заканчивается код попадает в конец очереди задач.
Неважно даже если укажем 0 секунд
	-Task Queue
		*callback setTimeout
		*callback setInterval
		*обработчики событий
		*результаты fetch
Получается event loop всегда делает одно и тоже проверяет стек, если пуст берет новую задачу из очереди и кладет в стек на выполнения.
3)ООП Объектно-Ориентированное Программирование
	-Основные принципы
		*Инкапсуляция
		Скрыть данные манипулировать только через getter-ы setter-ы
		*Наследование
		наследовать данные из одного класса в другой
		Если класс Manager наследует данные у Employee, то соответственно наседуются все поля и методы.
		*Полиморфизм
		Один и тот же метод может работать по разному в разных классах
В JS объекты наследуют свойства от других объектов, а не от классов.
у объекта родитель prototype, все методы объекта в prototype или у другого прототипа, которая является родительем предыдущего прототипа.
	{} -> prototype -> prototype -> prototype
	-массивы — объекты
	-функции — объекты
	-даты — объекты
	-регулярки — объекты
	-классы — тоже объекты
	-даже ошибки — объекты
	-даже почти все внутренние сущности — объекты
4)С момента ввода URL в адресную строку начинается проверка кеша и предзагрузка.
	HTTP-кеш (файлы и страницы, которые уже сохранены после последнего визита)
	Service Worker (если сайт использует PWA)
	DNS-кеш (ранее разрешённые домены браузер помнит, какой IP-адрес у сайта)
	Проверка локального DNS-кеша.
	Если нет, запрос к DNS-серверу провайдера.
	DNS возвращает IP-адрес сервера.
	TCP-соединение через порт.
	TLS/SSL (если HTTPS)
	Отправка HTTP-запроса
	Ответ сервера
	Разбор HTML и построение DOM
	CSSOM и рендеринг
	Выполнение JavaScript
	Междоменное взаимодействие (CORS) и безопасность
	Отображение страницы пользователю
